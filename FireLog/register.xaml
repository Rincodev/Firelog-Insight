<Window x:Class="FireLog.register"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register" Height="450" Width="800" WindowStartupLocation="CenterScreen" Background="#f4f4f4">
    <Grid x:Name="MainGrid">
        <Border Background="White" CornerRadius="10" Padding="40" Width="400" Height="350" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Please Sign in!" FontSize="24" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <TextBlock Text="Username" Margin="0,0,0,5" Foreground="#666"/>
                    <TextBox Name="UsernameTextBox" Height="30" Padding="5" Background="#f0f0f0" BorderThickness="0" Margin="0,0,0,15"/>

                    <TextBlock Text="Password" Margin="0,0,0,5" Foreground="#666"/>
                    <PasswordBox Name="PasswordBox" Height="30" Padding="5" Background="#f0f0f0" BorderThickness="0" Margin="0,0,0,25"/>

                    <Button x:Name="btn_SignIn" Content="Sign In" Height="35" Background="#0078D7" Foreground="White" BorderBrush="{x:Null}" FontWeight="Bold" Cursor="Hand" Click="btn_SignIn_Click"/>

                    <TextBlock x:Name="txt_CreateAccount" Text="Already have an account? Log in" Foreground="#666" FontSize="12" HorizontalAlignment="Center" Margin="0,20,0,0" Cursor="Hand"/>
                </StackPanel>

                <Button x:Name="btn_LogIn" Content="button na Log in"
                        Width="200" Height="25"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Center"
                        Margin="0,245,0,0"
                        Click="btn_LogIn_Click"
                        Background="#e0e0e0"
                        Foreground="#333"
                        BorderBrush="#ccc" Opacity="0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
